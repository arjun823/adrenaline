# Cargo configuration for Adrenaline compiler

[build]
# Use release profile by default
jobs = 4

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true  # Strip symbols to reduce binary size
